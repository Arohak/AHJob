# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AHJob' do
  use_frameworks!

  pod 'SwiftSoup'
  pod 'Reusable'
  pod 'PureLayout'
  pod 'Fabric'
  pod 'Crashlytics'
end
